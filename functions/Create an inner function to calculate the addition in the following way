def outer_fun(a, b):
    square = a ** 2
    
    def addition(a, b):
        return a + b

    add = addition(a, b)
    # add 5 to the result
    return add + 5

result = outer_fun(5, 10)
print(result)
